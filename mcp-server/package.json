{
  "name": "mental-model-mcp-server",
  "version": "1.0.0",
  "description": "HTTP MCP server (SSE transport) for the not-mental-model knowledge graph",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
